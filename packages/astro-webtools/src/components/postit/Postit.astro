---
// Copyright (c) Pascal Brand
// MIT License

import './webtools-postit.scss'
import { webtools } from '../../index'
import type { HTMLAttributes } from 'astro/types';

const propsToVars: webtools.PropsToVars = [
  { var: '--wt-polaroid-R',      prop: "up"},
  { var: '--wt-polaroid-rotate', prop: "rotate"},
  { var: '--wt-polaroid-width',  prop: "width"},
  { var: '--wt-polaroid-color',  prop: "color"},
]

interface PrivateProps {
  text: string,
  up?: string,
  rotate?: string,
  width?: string,
  color?: string | undefined | null,
}

interface Props extends HTMLAttributes<"div">, PrivateProps {}

const {
  class: className = '',
  style = '',
  text,
  ...props
} = Astro.props;

---
<div
  style=`${webtools.createVarStyle(style, props, propsToVars)}`
  class={className + " webtools-postit"}
  {...props}>
    {text}
</div>
